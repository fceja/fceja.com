pipeline {
    // define jenkins node/agent to be executed by
    agent { node { label env.NODE_AGENT_1 } }

    stages {
        stage('TODO - testing') {
            steps {
                // TODO - implement local testing
                echo "TODO - implement local testing"
            }
        }
    }
}
