node {
    def jenkinsfile

    if (env.BRANCH_NAME.equals('dev')) {
        jenkinsfile = 'dev.Jenkinsfile'
    } else {
        echo "Branch does not match any enviornment"
        return
    }

    load jenkinsfile
}